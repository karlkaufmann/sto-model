# Send-time prediction (mailing)

Repository for **research and evaluation** of a multiclass model that predicts **which discrete hour** (within allowed send windows) is associated with observed engagement, given features derived from the send timestamp and identifiers.

This repo is intentionally **minimal for public sharing**: notebooks, evaluation code, and documentation only. Raw data, archives, trained weights, and secrets are excluded via `.gitignore`.

---

## Problem statement

- **Task:** Multiclass classification over **10 ordered hour buckets** (mapped from class index 0–9 to wall-clock hours such as 7, 8, …, 20 depending on product rules).
- **Use case:** Support tooling that suggests **when to send** mailings, under the constraint that only certain hours are actionable.
- **Important nuance:** The model is trained on **historical behaviour** (conditional on your label construction). It is **not** a causal “best time” guarantee; offline metrics describe fit to the training distribution, not uplift.

---

## Repository layout

```
├── src/                        # All training source code, grouped by model version
│   ├── v2/                     # V2 model (CRM send-time features, 10 classes)
│   │   ├── feature_pipeline.py
│   │   └── train.py
│   ├── v3/                     # V3 model (recipient-profile features, 15 classes)
│   │   ├── feature_pipeline.py
│   │   └── train.py
│   ├── v3_2/                   # V3.2 unified model (V2+V3, NaN-aware, 10 classes)
│   │   ├── feature_pipeline.py
│   │   └── train.py
│   └── data/                   # Data utilities (merge, audit, inspect)
│       ├── merge.py
│       ├── audit.py
│       └── inspect_csv.py
├── serving/
│   └── server.py               # FastAPI inference server (all three model families)
├── evaluate/
│   └── evaluate_v2.py          # Batch evaluation script for V2
├── notebooks/
│   └── model_v2.ipynb          # End-to-end V2 exploration narrative
├── configs/                    # Feature configs per model version (tracked)
│   ├── v2/features.json
│   ├── v3/features.json
│   └── v3_2/features.json
├── results/                    # Training outputs (tracked)
│   ├── metrics/                # model_metadata per version (v1_baseline, v2, v3, v3_2)
│   ├── comparisons/            # Cross-version comparison JSONs
│   └── audits/                 # Data audit reports
├── docs/                       # Technical notes, deployment guides
├── models/                     # Trained artifacts — gitignored, generated by train.py
│   ├── v2/  (model.pkl, model.onnx, scaler.pkl)
│   ├── v3/  (model.pkl, model.onnx, scaler.pkl)
│   └── v3_2/ (model.pkl, model.onnx)
├── data/                       # Data files — gitignored
│   ├── raw/   (V2 zip, V3 CSV, cols.txt)
│   ├── processed/ (unified_v3_2.parquet)
│   └── eval/  (test_cases_data.csv, evaluated_test_cases.csv)
└── requirements.txt
```

Tracked content: `src/`, `serving/`, `evaluate/`, `notebooks/`, `configs/`, `results/`, `docs/`.
Gitignored: `data/`, `models/`, all `*.csv`, `*.pkl`, `*.onnx`, `*.parquet`.

---

## Environment

Use Python **3.10+** (3.12 is fine). Typical stack:

- `pandas`, `numpy`
- `xgboost` (training in notebook)
- `onnxruntime` (inference in `evaluate_model.py`)
- `scikit-learn` (splits / metrics in notebook)
- `onnxmltools` (optional export path in notebook)

Pin versions in your own `requirements-lock` for reproducibility; this repo does not ship a frozen lockfile by design.

---

## How to run

All scripts are run from the **project root** using Python's module flag (`-m`).

### 1. Notebook (`notebooks/model_v2.ipynb`)

1. Place your training CSV at `data/raw/nwl3_mailer_processed_view.csv` (gitignored).
2. Run cells top to bottom: load → features + target → train → evaluate blocks → export.
3. Inspect per-block accuracy and prediction histograms before promoting any export.

### 2. V2 batch evaluation

Expects `data/eval/test_cases_data.csv`, `models/v2/model.onnx`, `models/v2/scaler.pkl`.

```bash
python -m evaluate.evaluate_v2
```

Output: `data/eval/evaluated_test_cases.csv`.

---

### 3. V3 — recipient-profile model

Features: `email_domain`, `country`, `age`, `campaign_type`. Target: `open_hour_local` in `[7..21]` (15 classes).

```bash
python -m src.data.audit        # → results/audits/v3_data.json
python -m src.v3.train          # → models/v3/, results/metrics/v3.json
```

Known limitations: no absolute send timestamp → stratified random split only; personalization is segment-level.

### 4. V3.2 — unified model (V2 + V3, NaN-aware)

Merges V2 (CRM, sent-time features) and V3 (open-profile, NaN for sent-time). Single XGBoost model handles both modes via `tree_method=hist`.

**Schema mapping:**

| V3.2 field | V2 source | V3 source |
|---|---|---|
| `email_domain` | parsed from `recipient_email` | direct |
| `campaign_type` | `mailing_type` → mapped | direct |
| `country_iso2` | `nationality` → ISO-2 | `country` → ISO-2 |
| `sent_*` features | from send/open timestamps | NaN |
| `age` | NaN | direct (74% missing) |

```bash
python -m src.data.merge        # builds data/processed/unified_v3_2.parquet  (~30-60 min)
python -m src.v3_2.train        # trains, evaluates, exports                   (~20-40 min)
```

Outputs: `models/v3_2/`, `results/metrics/v3_2.json`, `results/comparisons/v2_vs_v3_vs_v3_2.json`.

Inference API — dual-mode endpoint:

```json
POST /predict/v3_2
{
  "email_domain": "gmail.com",
  "campaign_type": "newsletter",
  "country": "CZ",
  "sent_hour": 10,
  "sent_dow": 2
}
```

### 5. Inference server

```bash
uvicorn serving.server:app --host 0.0.0.0 --port 8000
```

Endpoints: `GET /health`, `GET /metrics`, `POST /predict` (V2), `POST /predict/batch`, `POST /predict/v3`, `POST /predict/v3_2`.

---

## Modelling notes (short)

- **Class imbalance:** Prefer stratified splits and consider **class weights** or block-level reporting so a majority hour does not mask weak blocks.
- **Feature–label leakage:** Ensure no column encodes the answer (e.g. future opens) when building training rows.
- **Train vs inference parity:** Feature order, dtypes (`float32` for ONNX), and **scaler type** must match what the exporter assumed; a mismatch often surfaces as collapsed predictions (e.g. one hour dominates).
- **Calibration:** Raw logits from ONNX are not always well-calibrated probabilities; treat “confidence” as a ranking aid unless you validate calibration on a holdout.

---

## Public repo hygiene

Before any commit:

1. Run `git status`.
2. Confirm no staged files match `.gitignore` deny patterns (data, zip, models, secrets).
3. Prefer the **allowlist** workflow described in `.agent/skills/public-repo-packaging/SKILL.md`.

---

## License

Specify your license in this section when you publish (e.g. MIT, Apache-2.0, or proprietary notice).
